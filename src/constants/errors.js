/* eslint-disable max-len */
'use strict';

const errorMessage = {
  onlyPOSTSupport: 'Compress support only POST request',
  needCompressFormat: 'Form is invalid. Need compress format',
  needFile: 'Form is invalid. Need file',
  unsupportedCompressFormat: 'Unsupported compress format',
};

module.exports = {
  errorMessage,
};
